# 🔍 Duplicate Questions Detection using NLP

**Duplicate Questions Detection** — проєкт для класифікації пар текстових запитань і передбачення **ймовірності**, що пара є дублікатом (має однаковий зміст).

## 2. Бізнес-задача та мета
Бізнес-задача — автоматично знаходити дубльований/схожий контент. Це корисно для:
- пошуку схожих назв товарів на маркетплейсах,
- виявлення дубльованих записів у базах клієнтів,
- групування повторюваних запитів у повідомленнях/підтримці.
Мета — побудувати максимально точну модель для прогнозу ймовірності дубліката.

## 3. Дані
- **Джерело:** Quora Question Pairs (Kaggle) — https://www.kaggle.com/c/quora-question-pairs  
- **Розмір:** Train ≈ 323 000 пар, Test ≈ 80 000 пар  
- **Опис:** `question1`, `question2`, мітка `label` (1 — дублікати, 0 — не дублікати).

## 4. Підхід до оцінювання, обрана метрика
Задача — бінарна класифікація з імовірнісним виходом.
- **Основна метрика:** LogLoss (якість прогнозованих ймовірностей)
- **Додаткові:** F1-score, Accuracy

## 5. Підхід до розв'язку, інструменти
- Baseline моделі на простих ознаках.
- Sentence-BERT embeddings для семантичних ознак.
- PCA для зменшення розмірності embeddings.
- Ознаки на основі embeddings: cosine similarity, |q1−q2| (абсолютна різниця), q1·q2 (поелементний добуток).
- Додаткові лексичні ознаки: TF-IDF cosine similarity, кількість спільних слів.
- Фінальний класифікатор: Logistic Regression (lbfgs).
Інструменти: Python, NumPy, Pandas, scikit-learn, sentence-transformers, Jupyter/Colab.

## 6. Результати (таблиця експериментів)
| Модель                                   | Ознаки                                                                 | LogLoss |  F1   | Accuracy |
|------------------------------------------|------------------------------------------------------------------------|---------|-------|----------|
| Baseline LogReg                          | TF-IDF cosine similarity між question1 та question2                    | ~0.47   | ~0.71 | ~0.77    |
| LogReg + SBERT (повні ознаки)            | Sentence-BERT embeddings (384) + cosine similarity + |q1−q2| + q1·q2  | ~0.41   | ~0.74 | ~0.78    |
| SBERT + агрегати                         | Sentence-BERT embeddings + агрегати різниці та добутку (mean, max, L2) | >0.41   | <0.74 | ~0.77    |
| SBERT + PCA (64)                         | Sentence-BERT → PCA (64) + |q1−q2| + q1·q2 + cosine similarity          | ~0.398  | ~0.757| ~0.80    |
| **SBERT + PCA + TF-IDF + common words**  | **PCA(64) + |q1−q2| + q1·q2 + TF-IDF cosine + кількість спільних слів** | **0.3875** | **0.7640** | **0.81** |



## 7. Висновки
Найкращий результат дала комбінація **Sentence-BERT + PCA** (для стабільності та зменшення розмірності) та простих лексичних ознак (**TF-IDF cosine** і **кількість спільних слів**). Фінальна модель має найнижчий LogLoss і найвищий F1 серед протестованих варіантів.

Практичний інсайт, отриманий у ході експериментів, полягає в тому, що семантичні embeddingи добре виявляють схожість за змістом, але можуть переоцінювати подібність між різними формулюваннями. 

Додавання лексичних ознак дозволяє зменшити кількість хибнопозитивних спрацьовувань, оскільки такі ознаки враховують фактичний перетин слів між питаннями. Застосування PCA додатково спростило простір ознак і зробило модель більш стійкою до шуму.

З точки зору бізнес-застосувань, запропонована модель може використовуватися для автоматичного виявлення дубльованого контенту в різних системах: об’єднання схожих запитів користувачів, очищення клієнтських баз даних, пошуку схожих назв товарів або повторюваних звернень у службах підтримки. Модель працює швидко, не потребує складного донавчання та може бути легко інтегрована в існуючі інформаційні системи.

Основні обмеження моделі пов’язані з тим, що вона не є end-to-end нейронною мережею і не навчається безпосередньо на текстах. Якість прогнозів значною мірою залежить від якості попередньо отриманих embeddings та вибраних ознак. Крім того, модель може помилятися у випадках, коли запитання мають однаковий зміст, але зовсім різну лексику.

Подальші напрями покращення можуть включати fine-tuning BERT-моделі під конкретну задачу, використання cross-encoder підходів для глибшого аналізу пар текстів або застосування більш складних моделей класифікації. Також можливим є розширення набору лексичних ознак та адаптація моделі під конкретні домени бізнесу.

## 8. Як запустити проєкт (Installation & Usage)
```bash
git clone <repository_url>
cd duplicate-questions-detection
pip install -r requirements.txt
jupyter notebook
